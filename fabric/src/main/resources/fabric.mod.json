{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": ["${mod_authors}"],
  "license": "${mod_license}",
  "environment": "*",
  "entrypoints": {
    "main": ["dev.rinchan.killconfirmed.fabric.KillConfirmedFabric"],
    "client": ["dev.rinchan.killconfirmed.fabric.KillConfirmedFabricClient"]
  },
  "mixins": ["kill_confirmed.mixins.json"],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_api_version}",
    "minecraft": "${minecraft_version}",
    "java": ">=21",
    "rinlib": ">=${rinlib_version}"
  }
}
